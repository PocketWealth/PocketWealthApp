<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing Registered Account Limit</h1>

  <%= turbo_frame_tag "registered_account_limit_#{@edit_type}" do %>
    <%= render partial: "form", locals: {type: @edit_type, registered_account_limit: @registered_account_limit} %>
  <% end %>

  <%= link_to "Back to limits", registered_account_limits_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
